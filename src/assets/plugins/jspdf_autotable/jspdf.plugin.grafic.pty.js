/** 
 * js?2017.1.0.1PDF AutoTable plugin v2.0.35
 * Copyright (c) 2014 Simon Bengtsson, https://github.com/simonbengtsson/js?2017.1.0.1PDF-AutoTable 
 * 
 * Licensed under the MIT License. 
 * http://opensource.org/licenses/mit-license 
 * 
 * @preserve 
 */
(function (jsPDF) {
    'use strict';

    jsPDF = 'default' in jsPDF ? jsPDF['default'] : jsPDF;

    // The current Table instance

    /**
	 * Create a table from a set of rows and columns.
	 *
	 * @param {Object[]|String[]} headers Either as an array of objects or array of strings
	 * @param {Object[][]|String[][]} data Either as an array of objects or array of strings
	 * @param {Object} [options={}] Options that will override the default ones
	 */
    jsPDF.API.ptygrafig = function (doc) {
        alert(doc)
 
    };

}(jsPDF));